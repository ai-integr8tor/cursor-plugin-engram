{
  "mcpServers": {
    "local-recall": {
      "command": "local-recall",
      "args": ["mcp"],
      "env": {
        "LOCAL_RECALL_DATA_PATH": "${LOCAL_RECALL_DATA_PATH}",
        "LOCAL_RECALL_WORKSPACE": "${LOCAL_RECALL_WORKSPACE}"
      }
    }
  }
}
